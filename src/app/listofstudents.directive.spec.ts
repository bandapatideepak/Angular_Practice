import { ListofstudentsDirective } from './listofstudents.directive';

describe('ListofstudentsDirective', () => {
  it('should create an instance', () => {
    const directive = new ListofstudentsDirective();
    expect(directive).toBeTruthy();
  });
});
